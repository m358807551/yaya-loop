{
  "id": "F004",
  "description": "Display a checkbox before each Todo and toggle its `done` state when selected. A Todo with `done = true` uses strikethrough text at 50% opacity.",
  "acceptance_criteria": [
    "Each Todo row renders a checkbox that reflects its current done state",
    "Selecting a checkbox toggles the state and provides immediate visual feedback",
    "When done is true, the Todo text uses strikethrough and opacity 0.5",
    "State changes persist through saveTodos",
    "The completion state remains after a page refresh"
  ],
  "source": "product/01-tasks.md#functional-flow",
  "notes": ""
}
