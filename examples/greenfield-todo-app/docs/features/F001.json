{
  "id": "F001",
  "description": "用 Vite + React 18 + TypeScript 模板创建项目；首次 npm run dev 能在浏览器打开一个空白页（标题为 TodoMate）。",
  "acceptance_criteria": [
    "npm install 成功无报错",
    "npm run dev 启动开发服务器，浏览器打开默认页面",
    "页面标题为 TodoMate（修改 index.html 的 <title>）",
    "页面 body 内只有一句 <h1>TodoMate</h1>（占位）",
    "npm run typecheck 通过零错误",
    "tsconfig 启用 strict 模式"
  ],
  "source": "infrastructure",
  "notes": "用 `npm create vite@latest todomate -- --template react-ts` 创建。tsconfig 已带 strict。删除模板自带的 Counter 示例。Code smell scan: 0 must_fix, 0 suggest, 0 acceptable（首次创建无业务代码）。"
}
