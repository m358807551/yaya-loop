{
  "id": "F001",
  "description": "Create the project from the Vite, React 18, and TypeScript template. The first `npm run dev` must open a minimal page titled TodoMate in the browser.",
  "acceptance_criteria": [
    "npm install completes without errors",
    "npm run dev starts the development server and the default page opens in a browser",
    "The page title is TodoMate, set through the <title> element in index.html",
    "The page body contains only the placeholder <h1>TodoMate</h1>",
    "npm run typecheck completes with zero errors",
    "tsconfig has strict mode enabled"
  ],
  "source": "infrastructure",
  "notes": "Create the project with `npm create vite@latest todomate -- --template react-ts`. The generated tsconfig already enables strict mode. Remove the template Counter example. Code smell scan: pass (feature: F001, must_fix: 0, suggest: 0, acceptable: 0)."
}
