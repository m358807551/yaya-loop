# AI 辅助开发 · 代码最佳实践

> 本文件用于约束 AI 在协助开发项目时的代码产出与协作行为。AI 应在每次会话开始时通读，并在产出代码或方案时主动遵循。
>
> **结构说明**：本文件分为四层。第一层是**协作契约**（与技术栈无关，最高优先级）；第二层是**通用设计模式与架构原则**（语言、引擎无关）；第三层是**引擎/平台最佳实践**（通过外部文件引入）；第四层是**编程语言最佳实践**（通过外部文件引入）。越往后越具体，发生冲突时以更高层为准——除非更具体层有平台/引擎的硬性约束。

---

# 第一部分 · 协作契约

> 这部分定义"AI 怎么和我一起干活"，与具体技术栈无关，优先级最高。

## 1.1 协作纪律

1. **不过度设计**：默认按"刚好够用"的复杂度实现。除非用户明确要求扩展性，否则不要预先抽象未被使用的接口、不引入未被需要的设计模式、不为"将来可能"的需求留扩展点。
2. **先确认，再动手**：需求模糊或存在多种合理解读时，先列出理解和疑问让用户确认，不要凭猜测大段写代码。
3. **小步推进**：一次改动聚焦一个目标。修复 Bug 时不顺手重构，重构时不顺手加功能。
4. **保持一致性优先于"更好"**：项目中已有的命名风格、目录结构、模式选型即是事实标准。不要因为个人偏好替换为"更优雅"的写法。
5. **诚实标注不确定性**：拿不准的 API、版本差异、平台行为，明说"需要验证"，不要编造。
6. **删除即回答**：被用户否决的设计或代码，下次不要再以变体形式提出。
7. **不擅自扩大改动范围**：用户让改 A，就只改 A。看到 B 有问题，先告知，由用户决定是否一起改。

## 1.2 工作流约束

- 实现 feature 时必须通过 `execute-next-feature` skill 走完整流程，不允许跳过预检查或人工验证阶段直接写代码。
- AI 不能自行将 feature 标记为 done，必须经人工验证后由人类口头/文字确认。
- AI 可以在非 main 分支上按项目 workflow 自动执行 `git commit`；`push` / `merge` / `reset` 等高风险操作仍需人类明确确认。
- 占位资源必须以 `_placeholder_` 为前缀命名，并在 feature 的 notes 中登记。
- 工作区不干净时不开新 feature。

**执行 `execute-next-feature` skill 时，阶段 0 出关报告（含规则原文 + 行号引用）和阶段 6 代码气味扫描（子 agent 委派）是流程硬约束，不是可选步骤。AI 跳过任一阶段都视为流程违规。最终 commit message 必须包含当前 feature id 且 `must_fix: 0` 的完整 `Code smell scan: pass` 证据行，否则会被 hook 阻断。**

## 1.3 AI 产出代码的提交规范

AI 在生成或修改代码时，应一并提供：

1. **变更摘要**：一句话说清这次改了什么、为什么改。
2. **影响范围**：列出受影响的文件和模块；如果改动涉及行为变化，明确指出。
3. **未做的事**：刻意没做、但用户可能期待的事，列出来。例如："只实现了主流程，错误分支未处理；如需要请告知。"
4. **验证建议**：手动测试步骤、或者建议运行的测试命令。

## 1.4 何时偏离本文档

本文档不是教条。以下情况可以偏离，但需在 AI 输出中明确说明原因：

- 用户明确要求另一种做法
- 项目已有的代码风格与本文档冲突，且修改成本过高
- 特定平台/引擎的惯例与本文档冲突（应优先遵循平台惯例）

偏离时，AI 应一句话告知："此处偏离了实践文档的 X 条，原因是 Y。"

---

# 第二部分 · 通用设计模式与架构原则

> 这部分与编程语言、引擎无关，是思想层面的约束。

## 2.1 首选模式：命令模式（Command Pattern）

> **凡是"做一件事"的逻辑，优先封装成命令类。** 这是本方法论最重要的代码组织原则之一。

### 2.1.1 核心思想

"做一件事"的本质，是**从一些输入出发，经过若干中间值的求解，最终得到一个结果**的线性过程。命令模式把这个过程显式地表达为一个类。

### 2.1.2 标准结构

一个命令类应该长这样：

- **类名是一个动词短语或"动词 + 名词"**：`RocketMaker`、`DamageCalculator`、`PathFinder`、`SaveLoader`。看到类名就知道它在做什么。
- **构造函数接收输入参数**，并保存为实例属性。
- **构造函数同时声明所有中间变量**，初始化为空（`None` / `null` / 默认值）。这样从构造函数就能看出整个计算过程涉及哪些中间状态。
- **每个中间步骤是一个小方法**，每个小方法只求一个中间变量、或只做一件事。
- **暴露一个公开的 `do` 方法**（也可以叫 `run` / `execute`，项目内统一一种），按线性顺序调用那些小方法。简单的分支判断可以直接写在 `do` 里。
- **除构造函数外，其他方法一般不接收参数**，因为它们需要的数据要么是构造函数里的输入、要么是已经求出的中间变量。

### 2.1.3 示意（伪代码，语言无关）

```
class RocketMaker:
    def __init__(self, blueprint, materials):
        # 输入
        self.blueprint = blueprint
        self.materials = materials
        # 中间变量（先占位）
        self.validated_materials = None
        self.assembled_parts = None
        self.fuel_loaded = None
        # 输出
        self.rocket = None

    def do(self):
        self._validate_materials()
        self._assemble_parts()
        self._load_fuel()
        self._finalize()
        return self.rocket

    def _validate_materials(self): ...
    def _assemble_parts(self): ...
    def _load_fuel(self): ...
    def _finalize(self): ...
```

### 2.1.4 为什么首选命令模式

- **类名即文档**：`RocketMaker` 已经说明了一切。
- **`do` 方法是流程的"目录"**：读 `do` 就能看到完整的执行顺序，不用跳来跳去。
- **每个小方法职责单一**：易于阅读、测试、替换。
- **中间变量集中在构造函数**：所有状态一目了然，不会散落在各处。
- **天然支持撤销、回放、录制、AI 接管**：命令对象本身就是一次操作的完整描述，可以序列化、存入历史栈。

### 2.1.5 何时不用命令模式

- 一行就能写完的纯计算（`max(a, b)` 这种）。
- 完全无状态、无中间变量的工具函数。
- 项目其他地方已有不同惯例，且修改成本过高（参考 1.1 第 4 条）。

## 2.2 其他核心架构原则

- **数据与表现分离**：通过观察者模式（或事件总线）让数据层不感知表现层，表现层订阅数据变化。数据可以独立运行（便于自测和命令行验证），表现层只是数据的一个"投影"。
- **状态用状态机管理**：任何有 ≥ 3 种状态、且状态间存在迁移规则的对象（角色、UI 流程、关卡进度），都用显式状态机表达，避免散落的 `if/else` 判断。
- **逻辑分层**：约定三层即可——数据/规则层、调度/控制层、表现/输入层。命名可以不叫 MVC，但层级关系要清晰：上层调用下层、下层通过事件回到上层，禁止跨层反向依赖。
- **组合优于继承**：游戏对象优先用组件组合（位置、渲染、碰撞、AI 各自独立），不要建大一统的基类继承树。
- **数据驱动**：能写在配置/数据里的就不写在代码里（关卡、技能数值、敌人参数）。改数值不应该需要改代码。
- **核心规则纯函数化**：判定、计算、得分规则等核心规则，应能脱离引擎、脱离 UI 直接调用并断言结果。
- **依赖通过参数传入**：依赖关系显式化，方便替换为测试桩、方便理解模块边界。
- **运行时状态可序列化**：核心状态应能随时被序列化为纯数据结构，便于存档、回放、远程调试。

## 2.3 常用模式速查表

> AI 应识别下列场景并主动套用，不必每次询问。

| 场景 | 首选模式 |
|------|---------|
| **做一件事（求输入到输出的过程）** | **命令模式** |
| 多个系统需要响应同一事件 | 观察者 / 事件总线 |
| 角色/UI 有多种状态且互相切换 | 状态机 |
| 玩家操作需要支持撤销、回放、录制、AI 接管 | 命令模式（同上） |
| 一类对象高频创建销毁（子弹、特效、伤害数字） | 对象池 |
| 多个系统都需要访问的全局服务（音频、存档、配置） | 服务定位器（避免裸单例） |
| 对象由不同能力拼装而成 | 组件 / ECS（轻量场景用组件即可，不必上 ECS） |
| 表现层只是数据的镜像 | 观察者 + 不可变数据快照 |
| 跨场景/跨模块通信，链路过长 | 事件总线（但要克制，全用事件会失去类型约束） |

## 2.4 反模式（明确避免）

- **God Object**：一个类管所有逻辑（典型如 `GameManager` 越长越大）。
- **逻辑写在 UI 回调里**：按钮点击事件里直接写业务逻辑，导致没法脱离 UI 测试。
- **裸单例满天飞**：每个系统都做成单例，导致依赖关系不可见、测试困难。
- **状态散落**：用一堆布尔变量 `isJumping`、`isAttacking`、`isDead` 表达状态，而不是状态机。
- **预先抽象**：只有一种实现就先抽个接口，"以备将来"——除非将来明确存在。
- **魔法数字嵌在逻辑里**：`if (score >= 5000)` 这类硬编码，应抽成具名常量或配置项。
- **过程式长函数代替命令类**：一个 200 行的 `process_xxx` 函数，应该被拆成一个命令类（参考 2.1）。

## 2.5 通用代码组织

1. **按功能分目录，而不是按文件类型**。`player/`（含数据、视图、控制）优于 `models/ views/ controllers/` 三个目录把同一功能拆散。
2. **单文件单职责**：一个文件一个核心概念。一个文件超过约 300 行就考虑拆分。
3. **入口最小**：主入口只做装配（创建系统、注入依赖、启动主循环），不写业务。
4. **配置集中**：所有可调参数集中在 `config/` 或 `data/`，不散落在各业务文件顶部。
5. **不要建 `utils.py` / `helpers.ts` 这类垃圾桶**：放进去的东西很快会失控。按主题命名工具文件（`math_utils`、`grid_utils`）。

## 2.6 通用错误处理原则

- **区分"预期错误"和"异常"**：用户点了不能点的按钮 → 静默忽略或提示；读取损坏存档 → 异常并记录日志。
- **不吞异常**：不要写空的 catch / except。至少要记日志，并明确说明为什么可以忽略。
- **资源加载失败要兜底**：图片、音频、配置缺失时给出占位资源或明确错误，不要白屏闪退。
- **断言用于不变式**：开发期用断言保护"绝不应该发生"的情况。发布版可关闭。

## 2.7 性能原则（默认不优化）

- **先测后改**：所有性能优化必须基于测量，禁止凭直觉优化。
- **对象池只用于真正高频的对象**（子弹、粒子）。低频对象用对象池只会增加心智负担。
- **不要为优化牺牲可读性**，除非确实是热点。
- 引擎相关的具体性能注意事项见第三部分。

---

# 第三部分 · 引擎 / 平台最佳实践

> 不同引擎/平台有不同的生命周期、节点系统、资源管理方式、调试工具。这部分通过引入外部文件提供具体规则。**切换引擎时，只需替换下方引入的文件即可。**

## 3.1 当前引擎 / 平台

{{ENGINE_NAME}}

请遵循: @docs/coding-rules/engine-rules.md

## 3.2 跨引擎通用考虑

优先使用引擎特性。举个例子，各个引擎有自带的寻路算法，那么优先使用引擎自带的寻路，而不是自己实现 A* 寻路算法。

### 游戏 / 实时循环与帧（如适用）

- **逻辑用固定时间步，渲染用变化时间步**：物理/AI 等可重现性敏感的部分按固定步长更新；插值用于平滑表现。回合制可以忽略此条。
- **delta time 显式传递**：所有 `update(dt)` 接收 `dt`，不要在内部读全局时间。
- **暂停只暂停逻辑层**：动画、UI 过渡仍可继续，避免暂停时界面僵硬。
- **不要在更新循环里做重活**：磁盘 IO、网络、大对象分配应避开热路径。

### 存档与版本

- **存档版本号**：从第一版就预留 `version` 字段，避免后续兼容噩梦。
- **随机数注入**：随机数生成器作为依赖传入需要它的系统，不直接调用全局 `random`。这样录像回放、单元测试才有可能。

---

# 第四部分 · 编程语言最佳实践

> 同一个引擎可能支持多种语言。这部分通过引入外部文件提供具体规则。

## 4.1 当前语言

{{LANGUAGE_NAME}}

请遵循: @docs/coding-rules/language-rules.md

## 4.2 跨语言通用的命名与可读性

> 这些规则在任何语言里都适用；语言特有的命名约定（如 Python 用 snake_case、C# 用 PascalCase）见各语言专属文件。

- **命名表达意图，不表达类型**：`enemies` 而不是 `enemyList`；`isAlive` 而不是 `aliveFlag`。
- **统一术语**：同一概念在代码各处用同一个词。例如确定用 `tile` 就不要混用 `cell` / `block` / `grid_unit`。AI 看到代码里出现同义词混用，应主动指出。
- **布尔命名以 `is_` / `has_` / `can_` / `should_` 开头**。
- **函数名是动词短语**：`spawn_enemy`、`apply_damage`，而不是 `enemy_spawn`、`damage_handle`。
- **命令类名是动名词或"动词 + 名词"**：`RocketMaker`、`DamageCalculator`（参考 2.1）。
- **避免缩写**：除非是行业通名（`pos`、`vel`、`hp`、`fps`），否则写全称。
- **注释解释"为什么"，不解释"是什么"**：代码本身已经说明了"是什么"。

## 4.3 跨语言通用的可测试性

- **核心规则纯函数化**（重申 2.2）：判定、计算、得分规则等核心规则，应能脱离引擎、脱离 UI 直接调用并断言结果。
- **依赖通过参数传入，而不是模块内 `import`**：方便替换为测试桩。
- **至少为核心规则写一组测试**：哪怕只有 5 个用例，也能在重构时救命。
- **AI 修改核心规则后应主动建议跑测试**，不要等用户提醒。
