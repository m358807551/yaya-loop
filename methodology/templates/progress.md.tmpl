# 项目进展

> 本文件是 long-running agent 的"工作记忆外置"。execute-next-feature 各阶段会持续追加内容，避免依赖上下文窗口。

## 当前正在做

*由 execute-next-feature 阶段 2 填入：当前 in_progress feature 的 id + title*

## 进展

*由 execute-next-feature 阶段 3 持续追加：每完成一个子步骤一行；遇卡点也记录在此*

## 上下文笔记

*跨多个 feature 的备忘、设计共识、临时决策。手工维护，execute-next-feature 不自动改这一区。*

## 历史

*由 execute-next-feature 阶段 7 归档：每完成一个 feature，把上方「当前正在做」+「进展」搬到这里，加上完成时间戳和 feature id*
