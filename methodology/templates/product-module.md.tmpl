# {{NN}}-{{module-name}} · {{MODULE_DISPLAY_NAME}}

## Module positioning

*In one or two sentences, explain this module's role in the system and what it interacts with.*

## Functional flow

*Describe the user or system behavior step by step. An ASCII diagram or numbered list is welcome.*

```text
1. ...
2. ...
3. ...
```

## Data model

| Field | Type | Default | Description |
| --- | --- | --- | --- |
| *field_name* | *string / int / bool / enum* | *default* | *Meaning of the field* |

## State machine (if applicable)

```text
state_a ──(condition)──→ state_b
state_b ──(condition)──→ state_c
```

## UI sketch

*The product-ui-sketcher skill produces the ASCII wireframe. Example:*

```text
┌──────────────────────────┐
│  [Action 1]  [Action 2]  │
│                          │
│  Content area            │
│                          │
└──────────────────────────┘
```

Key interactions:

- *Select Action 1 → ...*
- *Select Action 2 → ...*

## Audio entries

| Trigger | Filename | Style | Duration |
| --- | --- | --- | --- |
| *When action X occurs* | *_placeholder_sfx_X.wav* | *Brief and bright / heavy and muted / ...* | *0.3s* |

## Numeric rules

*Record formulas, thresholds, and configuration values. For example:*

- *Maximum value: 100*
- *Decay formula: current = max(0, current - rate * dt)*

## Acceptance criteria

*Each item must state a concrete way to verify the completed behavior.*

1. *In context X, the user performs Y and observes Z.*
2. *...*

## Edge cases

*Record known cases intentionally excluded from this version.*

- *Case A: Not handled yet; leave it for a later Feature.*

## Change history

- *YYYY-MM-DD*: Initialized the module.
