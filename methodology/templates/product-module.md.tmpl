# {{NN}}-{{module-name}} · {{中文模块名}}

## 模块定位

*一两句话：本模块在系统里的角色，与谁交互。*

## 功能流程

*用户/系统行为的逐步描述，可用 ASCII 图或编号列表。*

```
1. ...
2. ...
3. ...
```

## 数据模型

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| *field_name* | *string / int / bool / enum* | *默认* | *字段含义* |

## 状态机（如适用）

```
state_a ──(condition)──→ state_b
state_b ──(condition)──→ state_c
```

## UI 草图

*由 product-ui-sketcher skill 产出 ASCII 线框图。例：*

```
┌──────────────────────────┐
│  [按钮1]  [按钮2]         │
│                          │
│  内容区                   │
│                          │
└──────────────────────────┘
```

关键交互：
- *点击按钮1 → ...*
- *点击按钮2 → ...*

## 音效条目

| 触发时机 | 文件名 | 风格说明 | 时长 |
|---------|--------|---------|------|
| *动作 X 发生时* | *_placeholder_sfx_X.wav* | *短促清脆 / 沉闷有力 / ...* | *0.3s* |

## 数值规则

*公式 / 阈值 / 配置项。例：*

- *最大值：100*
- *衰减公式：current = max(0, current - rate * dt)*

## 验收标准

*每条是「做完后能怎么验证」的具体陈述。*

1. *用户在 X 场景下按 Y，观察 Z 发生*
2. *...*

## 边缘情况

*已知但本版本明确不处理的情况，写明留给后续。*

- *情况 A：暂不处理，留给后续 feature*

## 变更历史

- *YYYY-MM-DD*：模块初始化。
